define( [ 'qunit' ], function ( QUnit ) {

    'use strict';

    return function () {

        QUnit.module( 'osgAnimation' );

        QUnit.test( 'Animation', function () {
            ok( true );
        } );
    };
} );
