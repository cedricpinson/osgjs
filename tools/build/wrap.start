(function (root, factory) {
    if (typeof define === 'function' && define.amd) {
        // AMD.
        define([ 'Q', 'Hammer', 'Leap', 'vr' ], factory);
    } else {
        // Browser globals
        root.OSG = factory( root.Q, root.Hammer, root.Leap, root.vr );
    }
}(this, function ( Q, Hammer, Leap, vr ) {
